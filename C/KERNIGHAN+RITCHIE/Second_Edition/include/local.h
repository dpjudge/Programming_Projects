#define TRUE    1
#define FALSE   0
#define SUCCESS 0
#define ERROR   1

#define BLANK ' '
#define TAB   '\t'
#define EOL   '\n'

#define MIN(X, Y) (((X) < (Y)) ? (X) : (Y))
#define MAX(X, Y) (((X) > (Y)) ? (X) : (Y))
